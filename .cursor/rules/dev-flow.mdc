---
description: 用日常中文口语驱动开发流程——把"开始做 X / X 做完了 / 给我个 PR"等短语自动映射到 Linear 任务流转、分支命名、commit/PR 规范。
alwaysApply: true
---

# 开发流程口语化操作指南

> 这是项目级规则，作用：当用户用日常中文短语描述开发动作时，按下表把它翻译成具体的 Linear / Git / PR 操作。
> 永远先按"项目配置"中的 `team` 与 `repo` 限定操作范围。

## 项目配置（请直接在这里修改）

```yaml
project:
  name: "智能数据分析助理"
  team: "STE"                          # Linear team 的 key / id / name 任一即可
  repo: "steeljiang2025/AIChatBot"     # GitHub 仓库 owner/name
  repo_url: "https://github.com/steeljiang2025/AIChatBot.git"
```

后续所有 Linear 操作都默认 `team = project.team`；所有 git/PR 操作都基于 `project.repo`。

---

## 口语 → 动作映射（自上而下匹配，命中即执行；未命中则反问意图）

| 用户口语（含同义短语） | 我要做的动作 |
|---|---|
| "开始做 X" / "开搞 X" / "启动 X 这个功能" | 1) 用 `list_issues`（team + 标题模糊匹配 + assignee 不限）找 X；找不到则用 `save_issue` 新建（`team=project.team`、`assignee="me"`、priority 默认 Medium）。2) 用 `list_issue_statuses` 取该 team 的状态，调用 `save_issue` 把状态切到 **In Progress**。3) 输出推荐分支名 `feat/{TEAM}-{n}-{kebab}` 与首条 commit 前缀 `feat({TEAM}-{n}): …`。 |
| "看下我的活" / "本周做啥" / "我手头有什么" | `list_issues` with `assignee="me"`, `team=project.team`，按状态分组展示，标注优先级。 |
| "把 X 切到 review" / "X 提交 review" | 找到 X 的 Issue → `save_issue` 状态切 **In Review**。 |
| "X 做完了" / "X 搞定" / "关掉 X" | 找到 X → 切 **Done**；若已有 PR 草稿则提示去 GitHub merge。 |
| "暂停 X" / "X 先放一放" | 切 **Backlog**（默认）或询问用户具体目标状态。 |
| "给我个 commit message" / "我要提交" | 基于当前 In Progress 的 Issue，生成 Conventional Commits：`<type>({TEAM}-{n}): <subject>`，body 写动机/影响面。 |
| "起个 PR" / "帮我写 PR" | 输出 PR 标题 = 首条 commit；正文使用下方 PR 模板，并自动追加 `Closes {TEAM}-{n}`。`project.repo` 为占位符时拒绝并提醒用户先填。 |
| "为 X 加条评论：…" | `save_comment` 写到对应 Issue。 |
| "查 X 的进度" | `get_issue` 取详情 + `list_comments` 取最近 3 条评论汇总。 |
| "建个里程碑 …" / "拆成几个里程碑" | `save_milestone`，必要时配套 `save_issue` 拆任务挂到里程碑下。 |

未列入的口语先用一句话复述确认意图，再决定调用哪条动作。

---

## 分支命名约定

- 功能：`feat/{TEAM}-{n}-{kebab}`
- 缺陷：`fix/{TEAM}-{n}-{kebab}`
- 重构：`refactor/{TEAM}-{n}-{kebab}`
- 文档/测试/杂项：`docs/` `test/` `chore/` 同上结构
- `{n}` 取 Linear Issue 编号；`{kebab}` 取标题前 ≤5 个词转小写连字符

## Commit / PR 模板

单条 commit：`<type>({TEAM}-{n}): <subject>`

PR 正文：

```md
## Summary
- …

## Test plan
- [ ] …

Closes {TEAM}-{n}
```

---

## 阶段化分支策略（HARD RULE，自 Phase 3 起强制）

> 适用范围：从 **Phase 3 起**所有对 `main` 的写操作 **必须**经由独立分支 + Pull Request 完成。Phase 1 / Phase 2 已用 trunk-based 直推 main 完成，仅作历史参考；之后不再使用。

### 触发条件（满足任一即进入分支模式）

- 当前改动归属于 Phase 3 / Phase 4 的 Issue（STE-16 起的所有 Issue）
- 用户明确说"走 PR" / "起分支" / "拉个分支"
- 改动涉及 `backend/` 业务代码、Alembic 迁移、SQL 安全层、LangGraph 工作流、鉴权、SSE 接口
- 单次改动跨 ≥ 2 个 Issue 或可能影响他人正在进行的工作

### 强制流程

1. **开始做 X**：先回显"本任务将在分支 `feat/STE-{n}-{kebab}` 上完成，commit 不会直接落到 main"，待用户确认后再开工。
2. 落代码前先：
   ```bash
   git switch main && git pull --rebase
   git switch -c feat/STE-{n}-{kebab}
   ```
3. commit 全部落到该分支；**严禁** 在该模式下执行 `git commit` 到 main 或 `git push origin main`。
4. 代码完成时：
   ```bash
   git push -u origin feat/STE-{n}-{kebab}
   gh pr create --title "<commit subject>" --body "...Closes STE-{n}"
   ```
   推完 PR 后 **立即** 把 Linear Issue 切到 **In Review**。
5. PR 通过（自评 / 合作者 LGTM / CI 绿）后：
   ```bash
   gh pr merge --squash --delete-branch
   git switch main && git pull --rebase
   ```
   merge 成功后再把 Linear Issue 切到 **Done**。

### 禁止动作

- ❌ 分支模式下 agent 不得对 `main` 执行 `git commit` / `git push origin main`
- ❌ 不得跳过 **In Review** 直接把 Issue 从 In Progress 切到 Done
- ❌ 不得 `git push --force` 到 main；其他分支 force-push 也需用户明确许可
- ❌ 不得提交空白 body 的 PR；至少包含 `## Summary` 与 `Closes {TEAM}-{n}`

### Linear 状态联动

| Git/PR 动作 | Linear 状态 |
|---|---|
| 起分支 + 第一条 commit | **In Progress** |
| `gh pr create` 成功 | **In Review** |
| `gh pr merge` 成功 | **Done** |

若仓库已启用 Linear ↔ GitHub 集成，可由 Linear 自动联动；agent 仍要在每次状态变更前回显意图。

### 例外（允许直接对 main 操作）

- 纯文档/规则类一次性修复（README / `.cursor/**` / 注释错别字），且 diff ≤ 5 行
- 用户明确说「这次直接推 main」
- hotfix 紧急补丁，并由用户口头授权

其余情况一律强制走分支。

---

## 默认安全策略

1. **会写入 Linear** 的动作（`save_issue` 切状态 / 创建、`save_comment`、`save_project`、`delete_*`）执行前先回显"我打算做 …"让用户确认；轻量动作（加一条评论、单纯改状态）可直接执行后再报告结果。
2. 标题模糊匹配出多条 Issue 时**必须**列出候选让用户选，禁止凭空新建。
3. 涉及 git 分支 / commit / PR：**只输出建议命令**，不直接调用 shell 执行 `git push` 等动作；除非用户明确说"执行"或"帮我跑"。Phase 3 起的强制分支模式见上一节。
4. `project.repo` 仍是 `<owner>/<repo>` 占位符时，拒绝生成 PR/远端命令，并提示先在本规则顶部填写。
5. 状态/标签/里程碑等枚举值不要硬编码，先用对应的 `list_issue_statuses` / `list_issue_labels` / `list_milestones` 拉取实际值。
6. 当前改动若命中"阶段化分支策略"的触发条件，agent 必须显式提醒"现在进入分支模式"，并以分支模式的强制流程执行；用户没明确豁免前不得回退到 trunk-based 直推。

---

## 示例

- 用户：「开始做登录系统功能」
  我：在 Linear team `STE` 中查 "登录系统" → 没有则建 Issue（assignee=me）→ 待确认后切 In Progress → 输出 `feat/STE-12-login-system` 与 `feat(STE-12): scaffold login module`。
- 用户：「登录做完了」
  我：找到 STE-12 → 待确认后切 Done → 若 `project.repo` 已配置则给出 `gh pr merge` 建议命令。
- 用户：「看下我的活」
  我：列出 team=STE 下 `assignee=me` 的 Issue，按 In Progress / Todo / Backlog 分组。
- 用户：「开始做 STE-16 后端依赖」（Phase 3 任务）
  我：识别命中"阶段化分支策略"触发条件 → 回显"本任务进入分支模式，分支名 `feat/STE-16-backend-deps`，commit 不会落到 main，完成后用 PR + squash merge"；待用户确认后再切 Linear In Progress 与拉分支。
- 用户：「STE-16 写完了」
  我：先 `git push -u origin feat/STE-16-backend-deps` + `gh pr create` → Linear 切 **In Review** → 等用户/CI 通过后 `gh pr merge --squash --delete-branch` → Linear 切 **Done**。
