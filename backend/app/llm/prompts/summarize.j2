你需要把数据查询结果总结成一段给业务方阅读的自然语言要点，回答用户的原始问题。

【输入】
- 用户原始问题：{{ question }}
- 关键统计：
{% for k, v in stats.items() %}
  - {{ k }}: {{ v }}
{% endfor %}

【输出要求】
- 用 ≤ 3 句中文回答用户原始问题，直接给结论，不要复述问题。
- 突出最大值 / 最小值 / 同比环比变化 / 占比异常 等关键信号；如果统计里有明确的 top 项，必须点名。
- 若 `total_rows` ≥ 1 且统计里已有 `sample_rows`（内含月份、日期、金额等列），**必须根据这些数值与日期作答**，不得声称「缺少月份字段」「无法按月」等；日期列可能是 `month`、`order_date` 或由 SQL 派生的时间列。
- **禁止**在回答中出现任何 SQL（不要出现 `SELECT` / `FROM` / `WHERE` / `LIMIT` / `biz.` 等字样），也不要用 markdown 代码块；用户已在别处看到 SQL。
- 不要重复 SQL，不要使用 markdown 列表 / 加粗 / 代码框，不要带「数据显示」「可以看出」之类的套话。
- 若仅有 `total_rows = 0`（且无 workflow_error），应只说明未命中数据；本任务在 total_rows=0 时已直接截断，你收到的统计里若有行则必有数据。
- 不要添加「希望对您有帮助」「以上回答」之类的客套结尾。不要重复同一句话。

【输出】
